---
conditions:
  python: 3.12.13
  torch: 2.10.0+cpu
  torch_npu: 2.10.0.post2
domain: vllm-ascend
entry_id: 800f7613a97630e64f11f44089fb84cc8e1889dfa193b90a5ee5d9e0cdbdb1a2
kind: experience
schema: mindie-entry/2
summary: 'Small-scale layout check of torch_npu.npu_rms_norm on one logical Ascend NPU device: x[:, ::2] / weight[::2] non-contiguous path vs contiguous copies for torch.float16 and torch.bfloat16, each compared to a dtype-rounded FP32 CPU reference and the two outputs compared directly; result written to result.json and read back.'
title: torch_npu.npu_rms_norm non-contiguous view vs contiguous copy check on NPU for float16 and bfloat16
---

Process and remote setup:

- Goal: small-scale RMSNorm layout correctness check of torch_npu.npu_rms_norm on one logical NPU device.
- Target: pre-existing container (runtime_env=false), root=/mindie, independent directory /mindie/producer-cc-20260921; no container create/stop, no package installs, no other directories touched.
- Remote commands used `source /usr/local/Ascend/ascend-toolkit/set_env.sh` and exported ASCEND_RT_VISIBLE_DEVICES=0 before importing torch/torch_npu.
- A first proposed remote_write script was rejected during native approval: torch_npu.npu_rms_norm returns a tuple, so the output Tensor must be unpacked before calling detach. No remote write and no operator execution had occurred before that rejection. Only that return-value handling was corrected; no other rework loop.
- Corrected script written with native remote_write to /mindie/producer-cc-20260921/rmsnorm_layout_check.py (4748 bytes, sha256 76127ece28b8378103bdf87e512920af9f9ea8dbad7539a58d97a637e632da9c), then executed once with a single remote_bash (timeout_ms 90000, yield_time_ms 1000): `source /usr/local/Ascend/ascend-toolkit/set_env.sh && export ASCEND_RT_VISIBLE_DEVICES=0 && cd /mindie/producer-cc-20260921 && python3 rmsnorm_layout_check.py`.
- The bash call returned a running result with a session_id; it was continued only through that original job's stdin/status (no command re-execution) and finished with exit code 0.
- The script wrote result.json in the same directory; it was read back with native remote_read (sha256 656087b48abf756871579e8a5fbcf9ba93bce8a39c175dc82527f615086ce0f6, size 3962 bytes, 193 lines, partial=false).

Test setup: seed=20260923, epsilon=1e-6. Base input x is float32 [9,1024] and base weight float32 [1024]; for each dtype the base values are cast to the tested dtype on CPU first, then moved to NPU. The non-contiguous path uses x[:, ::2] -> [9,512] and weight[::2] -> [512]; the other path uses .contiguous() copies of the same values. Both paths call torch_npu.npu_rms_norm(x, weight, epsilon=1e-6), followed by torch_npu.npu.synchronize(). Reference is an FP32 CPU computation over the dtype-rounded view values: x * rsqrt(mean(x^2, dim=-1, keepdim=True) + 1e-6) * weight. Tolerance used for allclose: float16 atol=1e-3, rtol=1e-3; bfloat16 atol=1e-2, rtol=1e-2.

Observed tensor metadata (identical for both dtypes): input_view shape [9,512], stride [1024,2], is_contiguous=false, device npu:0; input_cont shape [9,512], stride [512,1], is_contiguous=true; weight_view shape [512], stride [2], is_contiguous=false; weight_cont shape [512], stride [1], is_contiguous=true; both output_view and output_cont returned shape [9,512], stride [512,1], is_contiguous=true. input_values_identical=true and weight_values_identical=true in both dtype cases.

Observed results (result.json, ok=true, error=null):

torch.float16:
- view_vs_ref_max_abs = 0.00366973876953125; view_vs_ref_allclose = true
- cont_vs_ref_max_abs = 0.00366973876953125; cont_vs_ref_allclose = true
- view_vs_cont_torch_equal = true; view_vs_cont_max_abs_diff = 0.0

torch.bfloat16:
- view_vs_ref_max_abs = 0.02512359619140625; view_vs_ref_allclose = true
- cont_vs_ref_max_abs = 0.02512359619140625; cont_vs_ref_allclose = true
- view_vs_cont_torch_equal = true; view_vs_cont_max_abs_diff = 0.0

The two paths were also compared directly (torch.equal on the two outputs) in addition to each path vs reference, as required by the task.

Software observed by the script inside the container: python 3.12.13, torch 2.10.0+cpu, torch_npu 2.10.0.post2, device_count 1, device_name Ascend910_9362, cann = null (neither /usr/local/Ascend/ascend-toolkit/latest/version.cfg nor /usr/local/Ascend/ascend-toolkit/latest/ascend_toolkit_install.info yielded content at the checked paths).

Command stderr observed: 'can not use command: npu-smi info' and '[W921 10:22:01.187502710 TensorFactories.cpp:340] Warning: Cannot create tensor with interal format while allow_internel_format=False, tensor will be created with base format. (function operator())'.

Limits: one small case per dtype ([9,512]); no other shapes, strides, or dtypes were exercised in this task; the CANN version was not retrieved; comparison covers each executed path against a dtype-rounded FP32 CPU reference and the direct two-path output comparison.
